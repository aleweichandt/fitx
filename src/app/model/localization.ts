import {addResource} from '../../localization';
import {STR_LOGIN_NS, strings as loginResources} from '../../login';
import {
  STR_ONBOARDING_NS,
  strings as onboardingResources,
} from '../../onboarding';

addResource(STR_LOGIN_NS, loginResources);
addResource(STR_ONBOARDING_NS, onboardingResources);
